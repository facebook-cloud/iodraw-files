```mermaid
graph TD
    A[产线专用电脑打开密钥灌装工具LineFlashing.exe] --> B[选择projectKey.bin文件]
    B --> C[点击密钥灌装按钮]
    C --> E[程序通过rsa2048公钥解密projectKey.bin内容得到16个字节密钥]
    E --> F[将密钥烧写到ECU的HSM]
    F --> G[从ECU中读取烧写的密钥和解密出的密钥进行对比]
    ；F --> H[解密出的密钥]
    G --> I{密钥对比}
    H --> I
    I --> J{相同?}
    J -- 是 --> K[提示灌装密钥成功]
    J -- 否 --> L[提示错误，需要重新灌装]

```